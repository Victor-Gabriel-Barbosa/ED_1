#include <stdint.h>
#include <stdio.h>

int main() {
  int8_t num = 123456789;
  printf("%lld\n", num); // This will print 123456789
  return 0;  // This function does not return a value, so it's not necessary to include return 0; in this case.
}